# novaAI
Nova is a retrieval-based AI chatbot built with Python, leveraging NLP techniques, sentence embeddings, and cosine similarity to match user queries with relevant information extracted from curated web sources. It exposes its functionality through a REST API using Flask.
